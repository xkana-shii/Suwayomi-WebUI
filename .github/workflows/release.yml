name: Release

on:
  workflow_dispatch:
defaults:
  run:
    working-directory: master

jobs:
  release:
    name: Create changelog, build artifacts and publish release
    runs-on: ubuntu-latest

    steps:
      - name: Cancel previous runs
        uses: styfle/cancel-workflow-action@0.12.1
        with:
          access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Checkout branch or tag that triggered workflow
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          path: master
          fetch-depth: 0

      - name: Set up NodeJs
        uses: actions/setup-node@v4
        with:
          node-version-file: './master/package.json'

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: |
            **/node_modules
          key: ${{ runner.os }}-${{ hashFiles('**/yarn.lock') }}

      - name: Generate Tag Name
        id: GenTagName
        run: |
          git fetch --tags
          latest=$(git tag --list 'v*' | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | sort -V | tail -n1)
          genTag=$(echo "${latest:-v0.0.0}" | awk -F. '{print $1"."$2+1".0"}')
          echo "::set-output name=value::$genTag"

      - name: Build webUI
        run: |
          yarn ci
          yarn build
          yarn build-zip
          yarn build-md5

      - name: Rename artifact
        run: |
          cd buildZip
          for file in *.zip; do
            mv "$file" "${file%-r$(git rev-list HEAD --count).zip}-${{ steps.GenTagName.outputs.value }}.zip"
          done

      - name: Create Tag
        run: |
          TAG="${{ steps.GenTagName.outputs.value }}"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"

          git tag $TAG
          git push origin $TAG

      - name: Upload Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "master/buildZip/*"
          owner: "xkana-shii"
          repo: "Suwayomi-WebUI"
          tag: ${{ steps.GenTagName.outputs.value }}
          draft: true
